"""Reliability of fault-tolerant remote primitives (GO/NO-GO study).

Circuits are built only from the TMCBS builder vocabulary (Stack, Wang, Mueller,
Nat. Commun. 17, 8838, 2026), so every noise location matches the published
simulator; this package composes those primitives into interaction patterns and
turns the measured logical error rates into per-operation costs.
"""
